# Agent Instructions

- Always include explicit type annotations for variables, parameters, and return values, even when the types could be inferred.
- Ensure every source file begins with the standard copyright header used elsewhere in the project (see existing files for reference).
